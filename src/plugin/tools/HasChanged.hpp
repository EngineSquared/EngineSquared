#pragma once

namespace ES::Plugin::Tools {
/**
 * @brief A struct representing if the object has changed
 *
 * @tparam  T   The type of the object
 */
template <typename T> struct HasChanged {
};
} // namespace ES::Plugin::Tools
